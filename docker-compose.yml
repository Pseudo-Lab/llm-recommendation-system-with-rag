version: "3.9"

services:
  api:
    container_name: api
    build:
      dockerfile: ./Dockerfile
    ports:
      - "8080:8080"
    volumes:
      - ./app:/code/app
    deploy:
      resources:
        limits:
          cpus: "${ES_CPUS}"
          memory: "${ES_MEMORY}"

